# Datum

Open datasets, open source — a CLI for publishing and consuming open datasets.

> Think "npm for data" or "git for datasets."

## Quick start

```bash
# Create a dataset metadata file
datum init

# Validate it
datum check

# Publish to a registry
datum publish
```

## Installation

```bash
pip install datum-cli
```

## Status

Early development. See the issue tracker for the roadmap.
